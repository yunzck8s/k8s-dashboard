# Agno 框架
agno>=0.1.0

# 数据库连接（Agno 使用 psycopg 而非 asyncpg）
psycopg[binary]>=3.0.0

# gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0

# HTTP 客户端
httpx>=0.27.0

# 配置和验证
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# 日志
structlog>=24.0.0

# 开发依赖
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
ruff>=0.3.0
