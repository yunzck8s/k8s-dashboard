# Font Assets

Self-hosted font files in use:
- `Lexend-Variable.ttf`
- `SourceSans3-Variable.ttf`
- `FiraCode-Variable.ttf`

Loaded by `src/index.css` via `@font-face` with `local()` + `url()` fallback.
